<h1>Nuevo grupo de imagenes</h1>

<%= form_with model: @image_group, url: admin_image_groups_path, local: true, html: { multipart: true } do |form| %>
  <div class="container-image-groups">
  <div class="form-group">
  <%= form.label :name %>
  <%= form.text_field :name, class: "form-control" %>
  </div>
   <div class="form-group col-md-4">
   <%= form.file_field :images, multiple: true, class: "form-control" %>
   </div>
   <div class="actions btn-group">
    <%= form.submit "Crear", class: "btn btn-primary" %>
    <%= link_to 'Atrás', admin_configuracion_sitio_path, class: "btn btn-secondary" %>
  </div>
  </div>
<% end %>